1. GROUP BY
=> 데이터를 그룹화하여 집계 함수(예: SUM, AVG, COUNT, MAX, MIN)를 사용할 때 유용합니다.
=> 특정 열의 값에 따라 결과 집합을 그룹화하고, 각 그룹에 대해 하나의 행을 반환합니다.


2. 규칙
2-1. group by에서 select 절에 포함된 칼럼은 그룹화의 기준이 되거나 집계 함수 내에 있어야 한다.


3. Having 절
3-1. where은 그룹화 하기 전에 조건을 적용하지만 Having은 group by가 적용된 뒤에 조건이 적용된다.
